apply plugin: 'java'
apply plugin: 'com.github.johnrengelman.shadow'

configurations { create 'shadow' }

shadowJar {
    zip64 = true

    configurations = [configurations.main, configurations.shadow]
}
